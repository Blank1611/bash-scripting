user id
ind 35,  nikhilshit
ip address
35.176.49.1
pass

Git bash
ssh ind35@35.176.49.1



~ec2-user

***
FTP Server IP address : 35.179.77.204
ftpuser
ftppassword

Connecting to Different machine 



ssh ec2-user@35.179.77.204

Ec2Password
***



'$' means regular user

'##' means root user

SU user:
 su devuser
 pass: devpass

cmmnds
ls, mkdir, cd, rm, rmdir(to del empty dirs)

touch command:-
touch file1.txt file2.txt

to remove directories recursively:
rm -r [dirname]


echo:(print command )

ca:(concatenates files to standard output)

back tip:
filenamevariable=/home/.bashrc
filecontent=`cat $filenamevariable`
echo $filecontent

$(variable)
"$variable"

Path variables:
 echo $PATH
  
Adding to path variable:

PATH=$PATH:/efs/dist/data/ref/1/common

Copy:(copies contents of file1 to file 2)
cp [file1] [file2]

Concurrency:
& for async process or background process

Ephemeral Port number:(Generated by system, random)

use "sdiff" for comparing files.
sdiff [file1] [file2]

File descriptors:
0-SI(Standart Input)
1-SO(Standard Output)
2-SE(Standard Error)

Sleep:
Sleep [time in seconds]  
e.g >> sleep 20

shopt command:
shopt -S huponexit

log files path:
/var/log

<&- closes the standard input
 
nohup command:

pstree -shapu
s-show parent
h-highlight specified process
a-arguments on cli
p-process id 
u-user id

ps x -f: all processes from all sesions
ps xr -f:running processes only
ps -ef 

status codes for process:
D-Uninterruptable sleep (Usually IO)
S- Interruptable sleep (waiting for an event to complete)
R-Running state runnable (On Run Queue)
T-stopped by job control signal (autosys)
t-stopped by debugger during the tracing 

LOOPS:

for loops:

for i in $list
> do
> echo i is $i
> done

done: to signal process is over

awk: for text processing

if then else :

 if [[ -z $ ]];then
> echo NULL_PID
> else
> echo PID_HAS+VALUE
> fi

square brackets recommended.

setting up lftp :

'bye' is exit

>>lftp ftp://ftpuser@[ipaddress]

put request:
lftp -c "open ftp://ftpuser@35.179.77.204;put linux.txt;cat linux.txt"


getUniverse function:


"""

#!/bin/bash
function usage {
        echo -e "usage: $0 VENDOR\neg: $0 LSE\nValid VENDOR's are LSE" >&2
        exit 1
}

test $# -eq 1||usage

VENDOR=$1
case $VENDOR in
  LSE);;
  *)usage
esac
echo `date +%T.%N` starting

#TODO setup ftp site on the nas with some sample data
OUTDIR=/data/ref/uni/$VENDOR
#lftp -c open ftp://.....;get uni.`logname`

echo downloading universe for $VENDOR
#simulate a large file/slow connection by sleeping for 20 seconds, so that can see the value in running job in the background
sleep 20
LOGNAME=`logname`
FILE=uni.$LOGNAME
echo "downloaded $OUTDIR/$FILE"
echo -e "VOD\nBP\nBT">$OUTDIR/$FILE

"""
 
test:
"""
 for i in {1..4}
> do
> cat $i.log
> done
"""

 for i in {1..4}; 
 do echo $(date -d -"$i"day); 
 bash getUniverse LSE 2>$i.log; 
 done
 
 """
 
 scp-secure copy
 